The mnist data csv columns are classLabel, pixel0, pixel1, pixel2... pixel783. So the first column is a character
0-9, and the rest is the input vector of pixels in 0-255.


model.csv: A model of doc2vec embeddings of all npr facebook data containing partisan terms "democrat", "republican", etc.
The fourth to last column are share counts for regression.
